<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} %-5level %c{1} - %msg%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Application code -->
        <Logger name="com.cs2predictor" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Streaming query progress (batch timing, input rows, latency) -->
        <Logger name="org.apache.spark.sql.execution.streaming.StreamExecution" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Silence noisy framework loggers -->
        <Logger name="org.apache.spark"   level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.apache.hadoop"  level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.apache.kafka"   level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.apache.parquet" level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="io.netty"           level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.apache.zookeeper" level="ERROR" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="ERROR">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
